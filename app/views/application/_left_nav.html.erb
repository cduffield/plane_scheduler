<%= render "account_menu" if Jumpstart.config.team_accounts? && user_signed_in? && Current.other_accounts.any? %>

<%# Place any links for the LEFT side of the nav bar here. For example: %>
<%#= nav_link_to "Dashboard", root_path %>
<% if user_signed_in? %>
  <%= nav_link_to "Airplanes", airplanes_path %>
  <%= nav_link_to "Events", events_path %>
  <%= nav_link_to "New Event", new_event_path %>
  <%= nav_link_to "Payment History", event_payments_path %>
<% end %>
